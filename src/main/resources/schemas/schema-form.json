{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "users": {
            "type": "array",
            "title": "Inline users",
            "description": "List of inline users",
            "items": {
                "type": "object",
                "title": "User",
                "properties": {
                    "username": {
                        "title": "Username",
                        "description": "Username. Supports deploy-time Expression Language only (API properties, dictionaries). Request-scoped variables are not available.",
                        "type": "string"
                    },
                    "password": {
                        "title": "Password",
                        "description": "Password resolved once at API deploy time from Expression Language and/or secrets. Request-scoped variables are not available. Secret rotation requires redeploying the API.",
                        "type": "string"
                    }
                },
                "required": ["username"]
            }
        }
    },
    "required": ["users"]
}
